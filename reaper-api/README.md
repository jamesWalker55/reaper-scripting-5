# Reaper ReaScript TypeScript starter template

This is an example repository for developing ReaScript plugins with TypeScript using [TypeScriptToLua](https://typescripttolua.github.io/).

```shell
bun install
bunx tstl --watch
```
